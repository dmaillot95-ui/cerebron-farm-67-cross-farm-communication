name: CEREBRON F67 Agora BUILD-002
on:
  push:
    paths: ['worker/agora_runtime.py','tests/test_agora.py','.github/workflows/agora-test.yml']
  workflow_dispatch:
jobs:
  agora-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python tests/test_agora.py

name: CEREBRON SAPHEA BUILD-039 Curriculum Compiler
on:
  push:
    paths: ['worker/curriculum_compiler.py','tests/test_curriculum_compiler.py','.github/workflows/saphea-build039-curriculum.yml']
  workflow_dispatch:
jobs:
  curriculum:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: python tests/test_curriculum_compiler.py
